<!DOCTYPE html>
<html>
  <head>
    <title>MtlOnlineMall</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" %>

    <!-- Inline CSS reset -->
    <style>
      html, body {
        margin: 0;
        padding: 0;
      }
    </style>
  </head>

  <body>
    <%= render "shared/navbar" unless controller_name == "pages" && action_name == "searchbar" %>

    <%= yield %>

    <%= render "shared/flashes" %>

    <% unless content_for?(:no_footer) %>
      <footer class="bg-dark text-white py-5 mt-5">
        <div class="container text-center">
          <h5 class="mb-4">Connect With Us</h5>
          <ul class="list-unstyled mb-4">
            <li>Email: <a href="hverar@gmail.com" class="text-white text-decoration-underline">hverar@gmail.com</a></li>
            <li>Instagram: <a href="https://instagram.com/kik3vr" class="text-white text-decoration-underline" target="_blank">@kik3vr</a></li>
            <li>Facebook: <a href="https://facebook.com/yourpage" class="text-white text-decoration-underline" target="_blank">MTL Mall</a></li>
          </ul>
          <p class="text-muted small mb-0">&copy; <%= Time.current.year %> MTL Online Mall. All rights reserved.</p>
        </div>
      </footer>
    <% end %>
  </body>
</html>
